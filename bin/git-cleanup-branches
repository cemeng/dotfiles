#!/bin/sh
#/ Usage: git cleanup-branches
#/ Delete all merged local branches, removing from Github too.
set -e

for b in `git branch --merged | grep -v master | sed 's/ //g'`; do
  git branch -d $b && git push origin :$b && git push joni :$b
done
